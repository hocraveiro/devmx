import { sharedApiInterfaces } from './shared-api-interfaces';

describe('sharedApiInterfaces', () => {
  it('should work', () => {
    expect(sharedApiInterfaces()).toEqual('shared-api-interfaces');
  });
});
